@using Graxei.Apresentacao.MVC4Unity.Areas.Administrativo.HtmlHelpers
@model LojaModel


@*//if (ViewBag.OperacaoSucesso != null)
    //{
    //<div class="alert alert-success alert-dismissable">
        //  
        <button aria-hidden="true" class="close" data-dismiss="alert" type="button">&times;</button>Loja incluída
    </div>
//}*@

@{
    AjaxOptions ajax = new AjaxOptions() { InsertionMode = InsertionMode.Replace, UpdateTargetId = "nova-loja", HttpMethod = "POST" };
    using (Ajax.BeginForm("Novo", "Lojas", null, ajax, new { @class = "form-horizontal" }))
    {
    @Html.Erro()
    @Html.Sucesso()
    <div class="form-group">
        @Html.ValidationMessageFor(m => m.LojaContrato.Nome)
        @Html.LabelFor(m => m.LojaContrato.Nome, new { @class = "control-label col-xs-1" })
        <div class="col-xs-5">
            @Html.TextBoxFor(m => m.LojaContrato.Nome, new { @class = "form-control" })
        </div>
        @{
        AjaxOptions ajaxOptions = new AjaxOptions() { UpdateTargetId = "listaEnd" };
        }
    </div>
    <div class="form-group">
        <div class="col-xs-offset-1 col-xs-1">
            <button type="submit" class="btn btn-primary">@Rotulos.Salvar</button>
        </div>
    </div>
    }
}

    <button id="novoEndereco" class="btn btn-primary">@Rotulos.NovoEndereco</button>
    <div id="divDoEndereco" class="alert-dismissable"></div>
@*<div>@Rotulos.Logotipo</div>
    <div>
        @if (Model.LojaContrato.Logotipo == null)
        {
            @Textos.NenhumLogo
        }
        else
        {
            <img width="150" height="150" src="@Url.Action("GetImagem", "Lojas", new { Model.LojaContrato.Id })" />
        }
        <div>@Rotulos.EscolherLogo<input type="file" name="Imagem" /></div>

    </div>
        <p>@Rotulos.Enderecos</p>
        @{Html.RenderPartial("AcoesEmLoja", Model);}
    *@